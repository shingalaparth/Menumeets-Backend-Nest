# Compiled by MenuMeets Agent
# .gitignore

# ------------------------------
# Dependencies
# ------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# Keep package-lock if you commit it, but user had `.package-lock.json` in ignore which is weird.
# Usually we DO commit package-lock.json. I will NOT ignore it unless user did.
# User had `.package-lock.json` in ignore, probably a typo or backup.
# I will ignore standard logs.

# ------------------------------
# Build / Dist
# ------------------------------
dist/
build/
out/
coverage/

# ------------------------------
# Environment Variables (CRITICAL)
# ------------------------------
.env
.env.production
.env.development
.env.test
.env.local

# ------------------------------
# OS Generated Files
# ------------------------------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ------------------------------
# IDE / Editor
# ------------------------------
.idea/
.vscode/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# ------------------------------
# Testing
# ------------------------------
/coverage
/junit.xml

# ------------------------------
# NestJS / TypeScript
# ------------------------------
*.tsbuildinfo
.eslintrc.js
# (Keep eslintrc if you config it, usually we commit it)

# ------------------------------
# Logs & Temp
# ------------------------------
logs/
*.log
temp/
tmp/
temp.txt
_audit/
_scans/
audit_results_*.json
parity_report*.md
verify_parity.js
universal_audit.js
read_audit.js

# ------------------------------
# Prisma
# ------------------------------
# Keep schema, migrations. Ignore generated client if output to default
# (default is inside node_modules so ignored automatically)
